diff --git a/lib/typescript/src/types.d.ts b/lib/typescript/src/types.d.ts
index 0d0e9082021ade2ecdbd7fadadfa4d9211561e13..ed09a4439f8d1495e366ee0fcd31ca8d575af38b 100644
--- a/lib/typescript/src/types.d.ts
+++ b/lib/typescript/src/types.d.ts
@@ -43,7 +43,32 @@ type IconMaterialSymbol = {
      */
     type: 'materialSymbol';
 } & Pick<MaterialSymbolProps, 'name' | 'variant' | 'weight'>;
-export type Icon = IconSfSymbol | IconMaterialSymbol | IconImage;
+type IconSFSymbolAsset = {
+    /**
+     * - `sfSymbolAsset` - Use a custom SF Symbol / image bundled in the app's
+     *   iOS asset catalog (Images.xcassets), referenced by name. The
+     *   app-provided counterpart to `sfSymbol`; resolved via `UIImage(named:)`.
+     */
+    type: 'sfSymbolAsset';
+    /** Name of the asset-catalog entry (e.g. a `.symbolset` or `.imageset`). */
+    name: string;
+};
+type IconDrawableAsset = {
+    /**
+     * - `drawableAsset` - Use a custom drawable bundled in the app's Android
+     *   resources (res/drawable), referenced by name. The app-provided Android
+     *   counterpart to `sfSymbolAsset`; resolved via a drawable resource id.
+     */
+    type: 'drawableAsset';
+    /** Name of the drawable resource (e.g. a generated VectorDrawable). */
+    name: string;
+    /**
+     * Whether the tab bar tints the icon: 'template' (default) tints with the
+     * item icon color; 'original' keeps the drawable's own (multicolor) colors.
+     */
+    tintingMode?: 'template' | 'original';
+};
+export type Icon = IconSfSymbol | IconMaterialSymbol | IconImage | IconSFSymbolAsset | IconDrawableAsset;
 export type HeaderSearchBarRef = {
     focus: () => void;
     blur: () => void;
diff --git a/src/types.tsx b/src/types.tsx
index a0a5deba89651c08f9595869e509e11c0ca9a2f0..516e2871e064d801cf96a5b3bd463daa6d487d28 100644
--- a/src/types.tsx
+++ b/src/types.tsx
@@ -56,7 +56,44 @@ type IconMaterialSymbol = {
   type: 'materialSymbol';
 } & Pick<MaterialSymbolProps, 'name' | 'variant' | 'weight'>;
 
-export type Icon = IconSfSymbol | IconMaterialSymbol | IconImage;
+type IconSFSymbolAsset = {
+  /**
+   * - `sfSymbolAsset` - Use a custom SF Symbol / image that is bundled in the
+   *   app's iOS asset catalog (Images.xcassets), referenced by name. The
+   *   app-provided counterpart to `sfSymbol`; resolved via `UIImage(named:)`.
+   */
+  type: 'sfSymbolAsset';
+  /**
+   * Name of the asset-catalog entry (e.g. a `.symbolset` or `.imageset`).
+   */
+  name: string;
+};
+
+type IconDrawableAsset = {
+  /**
+   * - `drawableAsset` - Use a custom drawable bundled in the app's Android
+   *   resources (res/drawable), referenced by name. The app-provided Android
+   *   counterpart to `sfSymbolAsset`; resolved via a drawable resource id.
+   */
+  type: 'drawableAsset';
+  /**
+   * Name of the drawable resource (e.g. a generated VectorDrawable like
+   * `nano_home`).
+   */
+  name: string;
+  /**
+   * Whether the tab bar tints the icon: 'template' (default) tints with the
+   * item icon color; 'original' keeps the drawable's own (multicolor) colors.
+   */
+  tintingMode?: 'template' | 'original';
+};
+
+export type Icon =
+  | IconSfSymbol
+  | IconMaterialSymbol
+  | IconImage
+  | IconSFSymbolAsset
+  | IconDrawableAsset;
 
 export type HeaderSearchBarRef = {
   focus: () => void;
