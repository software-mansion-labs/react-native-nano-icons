diff --git a/lib/module/views/BottomTabViewNativeImpl.js b/lib/module/views/BottomTabViewNativeImpl.js
index a1536bfad924ce09c75c07356d903743f5301fbc..59114f889398ecff0b8f69342dd8e06c4ce5159b 100644
--- a/lib/module/views/BottomTabViewNativeImpl.js
+++ b/lib/module/views/BottomTabViewNativeImpl.js
@@ -315,6 +315,9 @@ export function BottomTabViewNative({
           tabBarSelectionEnabled,
           tabBarBadgeStyle,
           tabBarIcon,
+          tabBarIconSize,
+          tabBarActiveIndicatorWidth,
+          tabBarActiveIndicatorHeight,
           tabBarBadge,
           tabBarSystemItem,
           tabBarBlurEffect = dark ? 'systemMaterialDark' : 'systemMaterial',
@@ -404,6 +407,13 @@ export function BottomTabViewNative({
           android: {
             icon: icon?.android ?? icon?.shared,
             selectedIcon: selectedIcon?.android ?? selectedIcon?.shared,
+            // Per-tab icon size (android-only). The bar uses the largest across
+            // tabs as its box; smaller ones are inset to their own size natively.
+            drawableIconSize: tabBarIconSize,
+            // Active-indicator size (android-only). Bar-wide: the largest across
+            // tabs wins; if unset it auto-scales to wrap the icon box.
+            activeIndicatorWidth: tabBarActiveIndicatorWidth,
+            activeIndicatorHeight: tabBarActiveIndicatorHeight,
             standardAppearance: {
               tabBarBackgroundColor: tabBarBackgroundColor ?? backgroundColor,
               tabBarItemRippleColor: currentOptions?.tabBarRippleColor,
@@ -537,6 +547,31 @@ function getPlatformIcon(icon) {
         },
         shared: undefined
       };
+    case 'sfSymbolAsset':
+      // A custom SF Symbol / image bundled in the app's iOS asset catalog,
+      // resolved by name via `[UIImage imageNamed:]` (react-native-screens'
+      // `xcasset` icon type). The app-provided counterpart to `sfSymbol`.
+      return {
+        ios: {
+          type: 'xcasset',
+          name: icon.name
+        },
+        android: undefined,
+        shared: undefined
+      };
+    case 'drawableAsset':
+      // A custom drawable bundled in the app's Android resources (res/drawable),
+      // resolved by name to a Drawable (react-native-screens' `drawableResource`
+      // icon type). The Android counterpart to `sfSymbolAsset`.
+      return {
+        ios: undefined,
+        android: {
+          type: 'drawableResource',
+          name: icon.name,
+          tintingMode: icon.tintingMode
+        },
+        shared: undefined
+      };
     case 'image':
       return {
         ios: icon.tinted === false ? {
diff --git a/lib/typescript/src/types.d.ts b/lib/typescript/src/types.d.ts
index 2450bdff4008492b388457cf3b4cba6fcce6468b..94d19a1fbcb2af32c5dd6eeea755422b4aa1d5f6 100644
--- a/lib/typescript/src/types.d.ts
+++ b/lib/typescript/src/types.d.ts
@@ -319,6 +319,31 @@ export type BottomTabNavigationOptions = {
      * Only supported on Android with `native` implementation.
      */
     tabBarLabelVisibilityMode?: TabBarItemLabelVisibilityMode;
+    /**
+     * Size (in dp) of this tab's icon. Can be set globally via `screenOptions`
+     * and/or overridden per screen; the largest value across tabs sets the bar's
+     * icon box, and smaller icons are inset to their own size. Unset tabs use the
+     * default 24dp.
+     *
+     * @platform android (native implementation)
+     */
+    tabBarIconSize?: number;
+    /**
+     * Width (in dp) of the selected tab's active-indicator pill. Bar-wide: the
+     * largest value across tabs is used. If unset, it auto-scales to wrap the
+     * icon box when icons are enlarged via `tabBarIconSize`.
+     *
+     * @platform android (native implementation)
+     */
+    tabBarActiveIndicatorWidth?: number;
+    /**
+     * Height (in dp) of the selected tab's active-indicator pill. Bar-wide: the
+     * largest value across tabs is used. If unset, it auto-scales to wrap the
+     * icon box when icons are enlarged via `tabBarIconSize`.
+     *
+     * @platform android (native implementation)
+     */
+    tabBarActiveIndicatorHeight?: number;
     /**
      * Style object for the tab label.
      */
